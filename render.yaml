services:
  - type: web
    name: fiverr-keyword-intelligence
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node src/server/server.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
